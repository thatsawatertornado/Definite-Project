chrome.runtime.onMessage.addListener(data => {
    switch (dataEvent) {
        case 'OnStop':
            break;
        case 'OnStart':
            break;
        default:
            break;
    }
})